# 2026-03-20 Update Log

## 반영한 작업

- `goal_reached` 판정을 latch 방식으로 안정화했다.
- Gazebo Classic용 obstacle world를 추가했다.
- `sim` 컨테이너 시작 시 `sim_assets/worlds/*.world`를 PX4 world 경로로 복사하도록 보강했다.
- 기본 world를 `obstacle_demo`로 바꿔 LiDAR가 기본 실행에서 finite obstacle 값을 볼 수 있게 했다.
- `metrics_logger`에 현재 obstacle 값(`current_obstacle_m`)을 추가해 artifact에서 바로 확인할 수 있게 했다.
- 팀 인수인계용 smoke test 스크립트 `scripts/smoke_test_single_drone.sh`를 추가했다.
- README에 baseline 검증 기준과 smoke test 실행 방법을 추가했다.
- experiment registry 자동화 스크립트를 추가했다.
- smoke test가 pass/fail, artifact 경로, issue/fix/notes를 실험 장부에 자동 기록하도록 보강했다.
- artifact 기반 SVG plot 생성기를 추가했다.
- 레거시 `offboard_control` 패키지를 제거하고, 문서를 현재 active path 기준으로 정리했다.
- 중복/외부 참고용 HTML과 일반론 문서를 정리해 docs를 현재 프로젝트 중심으로 축소했다.

## 추가/수정 파일

- [sim_assets/worlds/obstacle_demo.world](/home/deepblue/AV_Drone/sim_assets/worlds/obstacle_demo.world)
- [docker/sim/entrypoint.sh](/home/deepblue/AV_Drone/docker/sim/entrypoint.sh)
- [docker-compose.yml](/home/deepblue/AV_Drone/docker-compose.yml)
- [src/drone_planning/drone_planning/local_planner_node.py](/home/deepblue/AV_Drone/src/drone_planning/drone_planning/local_planner_node.py)
- [src/drone_metrics/drone_metrics/metrics_logger_node.py](/home/deepblue/AV_Drone/src/drone_metrics/drone_metrics/metrics_logger_node.py)
- [src/drone_bringup/config/drone1_autonomy.yaml](/home/deepblue/AV_Drone/src/drone_bringup/config/drone1_autonomy.yaml)
- [scripts/smoke_test_single_drone.sh](/home/deepblue/AV_Drone/scripts/smoke_test_single_drone.sh)
- [scripts/generate_artifact_plots.py](/home/deepblue/AV_Drone/scripts/generate_artifact_plots.py)
- [scripts/update_experiment_registry.py](/home/deepblue/AV_Drone/scripts/update_experiment_registry.py)
- [experiments/README.md](/home/deepblue/AV_Drone/experiments/README.md)
- [README.md](/home/deepblue/AV_Drone/README.md)
- [docs/architecture.md](/home/deepblue/AV_Drone/docs/architecture.md)
- [docs/docker-environment-spec.md](/home/deepblue/AV_Drone/docs/docker-environment-spec.md)
- [docs/command-reference.md](/home/deepblue/AV_Drone/docs/command-reference.md)

## 의도

- 팀에 넘기기 전 single-UAV baseline을 더 명확히 완성하기 위함
- “라이다 토픽이 보인다” 수준이 아니라, 장애물 world에서 실제 finite obstacle 값이 들어오는 baseline을 제공하기 위함
- 팀원이 동일한 기준으로 환경 상태를 빠르게 확인할 수 있게 하기 위함
- 반복 실험 결과가 취업/논문용 evidence chain으로 남도록 하기 위함

## 다음 검증 항목

1. `docker compose build sim` 후 `obstacle_demo` world가 실제로 로드되는지 확인
2. 최신 artifact에서 `mission_phase=HOVER_AT_GOAL`, `goal_reached=true`를 확인
3. 최신 artifact의 `closest_obstacle_m`, `pose_count`, `scan_count`가 정상인지 확인
4. smoke test 스크립트가 clean baseline에서 통과하는지 확인
5. `experiments/index.csv`, `experiments/scenario_table.csv`, `experiments/ledger.csv`가 자동 갱신되는지 확인

## 실제 검증 결과

2026-03-20 재검증에서 아래를 확인했다.

- `docker compose build sim` 완료
- `docker compose up -d sim ros` 완료
- `single_drone_autonomy.launch.py` 실행 후 상태 머신이 `OFFBOARD_ARM -> TAKEOFF -> HOVER_AFTER_TAKEOFF -> FOLLOW_PLAN -> HOVER_AT_GOAL`까지 진행
- `/drone1/scan` 실수신 확인
- smoke test 통과

검증 기준 artifact:

- [baseline_summary_example.json](/home/deepblue/AV_Drone/docs/examples/baseline_summary_example.json)

핵심 값:

- `mission_phase = HOVER_AT_GOAL`
- `goal_reached = true`
- `pose_count = 5468`
- `scan_count = 1829`
- `closest_obstacle_m = 0.20000000298023224`

주의:

- `current_obstacle_m`는 샘플 시점에 `Infinity`일 수 있다.
- obstacle world 검증 기준은 최신 시점의 거리보다 `closest_obstacle_m`가 finite였는지로 보는 것이 더 적절하다.
- 초기 smoke test 실패 원인은 `sim` 컨테이너가 내부 PX4/Gazebo build 중이거나 mission이 아직 진행 중인 시점에 너무 빨리 판정했기 때문이었다.
- smoke test는 이후 `sim` readiness와 `HOVER_AT_GOAL`, `goal_reached=true`까지 기다리도록 보강했다.
